<script>

  let {
    portableText,
    children,
  } = $props();

  let value = $derived(portableText?.value);

</script>

<span class="pseudo-p">{@render children?.() }</span>

<style lang='scss' >

  .pseudo-p {
    display: block;
  }

  .pseudo-p:not( :first-child ) {
    margin-top: 1em;
  }

</style>

