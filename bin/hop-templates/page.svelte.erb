<script>

  let { data } = $props();

  let metaTitle = $derived( data?.page?.title || 'Home' );
  let metaDescription = $derived( data?.page?.metaDescription );

</script>

<svelte:head>
  <title>{ metaTitle }</title>
  {#if metaDescription?.length }
    <meta name="description" content={ metaDescription } />
  {/if}
</svelte:head>

<h1>{ metaTitle }</h1>

