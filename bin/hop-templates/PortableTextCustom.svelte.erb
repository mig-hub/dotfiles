<script>

  import { PortableText } from '@portabletext/svelte';
  import PortableTextLink from "$lib/components/portabletext/PortableTextLink.svelte";
  import PortableTextInlineParagraph from "$lib/components/portabletext/PortableTextInlineParagraph.svelte";

  let {
    value = [],
    inline = false,
  } = $props();

  const componentsInline = {
    block: {
      normal: PortableTextInlineParagraph,
    },
  };

  const componentsBase = {
    // block: {
    //   h2: PortableTextH2,
    // },
    marks: {
      link: PortableTextLink,
    },
  };

  let components = $derived( inline ? { ...componentsBase, ...componentsInline } : componentsBase );

</script>

<PortableText
  {value}
  {components}
/>

